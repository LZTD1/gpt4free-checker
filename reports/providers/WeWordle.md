# WeWordle

- **Label:** WeWordle
- **URL:** https://chat-gpt.com
- **Models:** 7
- **Working tests:** 7 / 7
- **Avg response time:** 18.42s

## Per-model results

| Model | Capability | Status | Time | Notes |
| --- | --- | :---: | ---: | --- |
| `deepseek` | text | ✅ `ok` | 20.95s | contains expected token 'PONG' |
| `deepseek-r1` | text | ✅ `ok` | 0.89s | contains expected token 'PONG' |
| `deepseek-reasoner` | text | ✅ `ok` | 21.22s | contains expected token 'PONG' |
| `gpt-4` | text | ✅ `ok` | 1.67s | contains expected token 'PONG' |
| `gpt-4o` | text | ✅ `ok` | 21.02s | contains expected token 'PONG' |
| `gpt-4o-mini` | text | ✅ `ok` | 22.03s | contains expected token 'PONG' |
| `v3` | text | ✅ `ok` | 41.13s | contains expected token 'PONG' |

## Sample successful responses

### `v3` — text

```
PONG
```

### `gpt-4` — text

```
PONG
```

### `gpt-4o` — text

```
PONG
```

### `gpt-4o-mini` — text

```
PONG
```

### `deepseek` — text

```
PONG
```

### `deepseek-reasoner` — text

```
PONG
```

### `deepseek-r1` — text

```
PONG
```

